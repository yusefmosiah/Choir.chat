import Foundation
import SwiftUI

// Models are now split into:
// - ChoirAppModels.swift (UI Layer: Message, ChoirThread, Phase, ModelConfig, PhaseResult)
// - ChoirAPIModels.swift (API Layer: APIError, *APIResponse, *Data, *Response, AnyCodable, etc.)
